---
layout: <%= @app_module %>.PostLayout
<%= if @lang && @locale == "fr" do %>title: Bienvenue sur votre nouveau blog
date: 2026-01-01 12:00:00 +0000
permalink: /fr/blog/welcome/
description: Votre premier article sur un site Tableau propulsé par Corex.
<% else %><%= if @lang && @locale == "ar" do %>title: مرحبًا بك في مدونتك الجديدة
date: 2026-01-01 12:00:00 +0000
permalink: /ar/blog/welcome/
description: مقالك الأول على موقع Tableau مدعوم بـ Corex.
<% else %>title: Welcome to your new blog
date: 2026-01-01 12:00:00 +0000
permalink: <%= if @lang do %>/<%= @locale %>/blog/welcome/<% else %>/blog/welcome/<% end %>
description: Your first post on a Corex-powered Tableau static site.
<% end %><% end %><%= unless @lang do %>
tags:
  - Elixir
  - Tableau
  - Corex
<% end %>sitemap:
  priority: 0.8
  changefreq: monthly
---
<%= if @lang && @locale == "fr" do %>
Ceci est un article d'exemple généré par `mix corex.tableau.new`.

## Pour commencer

Votre site statique est propulsé par [Tableau](https://github.com/elixir-tools/tableau),
un générateur de sites statiques pour Elixir, avec les composants
[Corex](https://hexdocs.pm/corex) et les design tokens.

### Ajouter des articles

Créez des fichiers Markdown dans le répertoire `_posts/`. Chaque article a besoin d'un front matter :

```yaml
---
layout: <%= @app_module %>.PostLayout
title: Mon titre d'article
date: 2026-01-15 12:00:00 +0000
permalink: /fr/blog/mon-article/
description: Une courte description pour le SEO et les aperçus.
---
```

### Développement

Lancez le serveur de développement avec rechargement à chaud :

```bash
mix tableau.server
```

### Build de production

```bash
mix build
```

La sortie statique se trouve dans `_site/`. Déployez ce répertoire sur n'importe quel hébergeur statique.
<% else %><%= if @lang && @locale == "ar" do %>
هذا مقال نموذجي أنشأه `mix corex.tableau.new`.

## البدء

موقعك الثابت مدعوم بـ [Tableau](https://github.com/elixir-tools/tableau)،
مولّد مواقع ثابتة لـ Elixir، مع مكوّنات [Corex](https://hexdocs.pm/corex)
ورموز التصميم.

### إضافة مقالات

أنشئ ملفات Markdown في مجلد `_posts/`. يحتاج كل مقال إلى مقدمة (front matter):

```yaml
---
layout: <%= @app_module %>.PostLayout
title: عنوان مقالي
date: 2026-01-15 12:00:00 +0000
permalink: /ar/blog/my-post/
description: وصف قصير لتحسين محركات البحث والمعاينات.
---
```

### التطوير

شغّل خادم التطوير مع إعادة التحميل الحي:

```bash
mix tableau.server
```

### البناء للإنتاج

```bash
mix build
```

المخرجات الثابتة في `_site/`. انشر هذا المجلد على أي مضيف ثابت.
<% else %>
This is a sample blog post generated by `mix corex.tableau.new`.

## Getting started

Your static site is powered by [Tableau](https://github.com/elixir-tools/tableau),
a static site generator for Elixir, with [Corex](https://hexdocs.pm/corex) components
and design tokens.

### Adding posts

Create Markdown files in the `_posts/` directory. Each post needs front matter:

```yaml
---
layout: <%= @app_module %>.PostLayout
title: My Post Title
date: 2026-01-15 12:00:00 +0000
permalink: <%= if @lang do %>/<%= @locale %>/blog/my-post/<% else %>/blog/my-post/<% end %>
description: A short description for SEO and previews.<%= unless @lang do %>
tags:
  - Elixir
<% end %>
---
```

### Development

Start the dev server with live reload:

```bash
mix tableau.server
```

### Building for production

```bash
mix build
```

The static output lands in `_site/`. Deploy that directory to any static host.
<% end %><% end %>
