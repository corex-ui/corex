Phoenix + Corex web application.

- Run `mix precommit` before considering changes done.
- Prefer **Req** for HTTP; avoid `:httpoison`, `:tesla`, `:httpc`.
- Follow project conventions (e.g. no comments in code, where to put new modules).
